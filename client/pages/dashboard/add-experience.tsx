import React, { ReactElement } from "react";
import Layout from "../../components/Layout";

interface Props {}

export default function addExperience({}: Props): ReactElement {
  return <Layout>add experience</Layout>;
}
