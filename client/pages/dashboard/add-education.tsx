import React, { ReactElement } from "react";
import Layout from "../../components/Layout";

interface Props {}

export default function addEducation({}: Props): ReactElement {
  return <Layout>add education</Layout>;
}
